{
    "name": "franciscoteixeira/tiny-s3",
    "description": "A minimal AWS S3-compatible storage server written in pure PHP",
    "license": "MIT",
    "authors": [
        {
            "name": "Francisco Ernesto Teixeira"
        }
    ],
    "require": {
        "php": ">=8.5"
    },
    "require-dev": {
        "phpunit/phpunit": "^11",
        "guzzlehttp/guzzle": "^7"
    },
    "autoload-dev": {
        "psr-4": {
            "TinyS3\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "start":               "@php -S localhost:9000 index.php",

        "test":                "phpunit --no-coverage",
        "test:unit":           "phpunit --testsuite Unit",
        "test:integration":    "phpunit --testsuite Integration",

        "coverage":             "bin/coverage_text.sh",
        "coverage:html":        "bin/coverage_html.sh",
        "coverage:unit":        "phpunit --testsuite Unit --coverage-text=coverage/coverage.txt",
        "coverage:integration": "phpunit --testsuite Integration --coverage-text=coverage/coverage.txt",
        "coverage:ci":          "phpunit --log-junit coverage/junit.xml --coverage-text=coverage/coverage.txt"
    },
    "scripts-descriptions": {
        "start":               "Start the built-in PHP development server on :9000",

        "test":                "Run the full test suite (no coverage)",
        "test:unit":           "Run only the Unit testsuite (fast, no server)",
        "test:integration":    "Run only the Integration testsuite (starts a real PHP server)",

        "coverage":             "Run full suite with text coverage using Herd when available, otherwise standard Xdebug/PCOV",
        "coverage:html":        "Generate HTML coverage report using Herd when available, otherwise standard Xdebug/PCOV",
        "coverage:unit":        "Coverage for Unit tests only",
        "coverage:integration": "Coverage for Integration tests only",
        "coverage:ci":          "CI mode: coverage reports + JUnit XML in coverage/"
    },
    "config": {
        "process-timeout": 0
    }
}
