# Runtime secrets — never commit
.env

# Storage data — content only, preserve the directory
data/*
!data/.gitkeep

# Composer dependencies
vendor/

# PHPUnit / coverage artefacts
.phpunit.result.cache
coverage/

*.log
.python-version
.idea/
